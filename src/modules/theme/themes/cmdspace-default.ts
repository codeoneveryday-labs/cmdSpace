import type { Theme } from "../types";

export const cmdspaceDefault: Theme = {
  id: "cmdspace-default",
  name: "cmdSpace Default",
  description: "The default cmdSpace look — clean glass over neutral surfaces.",
  editorTheme: { dark: "atomone", light: "atomone" },
  variants: {
    light: {},
    dark: {},
  },
};
